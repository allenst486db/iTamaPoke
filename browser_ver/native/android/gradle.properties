# Required for any androidx.* dependency (appcompat/webkit/activity); the
# Android Gradle plugin refuses to configure the project without it.
android.useAndroidX=true
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
kotlin.code.style=official
