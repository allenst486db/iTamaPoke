# iTamaPoke

![Platform](https://img.shields.io/badge/platform-iPhone%20%2B%20Apple%20Watch%20%C2%B7%20Web%20app%20%C2%B7%20Android-black?logo=apple&logoColor=white)
![Code](https://img.shields.io/badge/code-custom%20license-blue)
![Status](https://img.shields.io/badge/status-personal%20project%2C%20bugs%20possible-yellow)

**[English README](README.md)**

[socquique/TamaPoke](https://github.com/socquique/TamaPoke) — 원형 AMOLED
보드용 1세대 포켓몬풍 다마고치 펌웨어 — 를 아이폰과 애플워치로 개인 빌드용
포팅한 프로젝트입니다. 게임 자체는 그대로입니다: 원본 하드웨어에서 돌아가던
그 C++ 코드가 여기서도 똑같이 돌아가고, 화면만 다를 뿐입니다.

> **개인 이용만 가능. 빌드를 다시 배포하거나 나눠주지 마세요 — 캐릭터를
> 바꾼 리스킨 버전(다른 캐릭터, 같은 엔진)도 동일하게 안 됩니다.** 받는
> 곳은 아래 링크 하나입니다. [라이선스](#라이선스) 참고.

**그냥 플레이하고 싶다면:** 폰에서 **https://allenst486db.github.io/iTamaPoke/**
를 여세요 — 아이폰: Safari → 공유 → *홈 화면에 추가*; 안드로이드: 같은
사이트의 [`iTamaPoke.apk`](https://allenst486db.github.io/iTamaPoke/iTamaPoke.apk)
다운로드. Mac도, 개발자 계정도, 만료도 없고 그 뒤로는 폰에서 오프라인으로
돌아갑니다. 캐릭터 그림은 직접 넣어야 합니다([설치
가이드](docs/INSTALL.ko.md#경로-d--링크에서-설치하는-웹앱-mac-없이-개발자-계정-없이-컴퓨터-없이)의
D3) — 사이트에는 일부러 하나도 안 들어 있습니다.

처음이신가요? 아래부터 시작하세요:

| | |
|---|---|
| 🎮 [게임 방법](docs/GAMEPLAY.ko.md) | 게임이 실제로 어떻게 동작하는지, 화면별로, 실제 화면 캡처와 함께 |
| 📲 [설치 가이드](docs/INSTALL.ko.md) | 코딩 지식 없이 따라하는 단계별 설명 — 웹앱/안드로이드 경로는 컴퓨터가 아예 필요 없고, 네이티브 iOS 경로는 Mac·Windows 둘 다 가능 |
| 🛠️ [Xcode 빌드 가이드](https://htmlpreview.github.io/?https://github.com/allenst486db/iTamaPoke/blob/main/docs/xcode_build_guide.html) | Xcode로 직접 빌드해서 설치하는 과정을 그림으로 |
| 🍎 [무료 vs 유료 Apple 계정](docs/DEV_ACCOUNT.md) | 어떤 설치 경로가 나에게 맞는지 |

> ⚠️ **이건 완성된 상용 제품이 아니라 개인이 취미로 포팅한 프로젝트입니다.**
> 특히 애플워치 쪽과 무료 계정 설치 경로(아직 검증 중)에서 일부 버그가
> 있을 수 있습니다. 뭔가 이상하게 보인다면, 이 단계에서는 있을 수 있는
> 일이지 사용자의 실수가 아닐 가능성이 큽니다.

---

## 상태

원작 대비 기능적으로 완성: 펌웨어의 모든 화면과 연출이 포팅되어 있고, 동일한
게임 로직 위에서 동작합니다.

**화면:** 실시간 낮/밤 하늘이 있는 대기 화면, 스타터 선택, 알과 부화, 먹이
메뉴, 목욕, 도감 갤러리(전체/키운 것/잡은 것 필터 포함), 8페이지 스탯
카드(프로필/성격/데일리 목표/박스/배틀/메달/진행도/원정), 승리 시 캐치
제안이 붙는 야생 배틀 화면(공격/회피/휴식), 5개 타일짜리 놀이 메뉴(공/
캐치/메모/청소/타입), 훈련 자루, 이름 변경 키보드, 설정, 그리고 진화/작별/
가출 결정과 그 연출까지 전부 포함됩니다.

**성격, 데일리 목표, 박스, 도감 필터, 배틀 시스템, 캐치/메모/청소/타입
미니게임, 원정, 그리고 효과음 합성기(칩튠 파형, 4단계 소리 모드)**는
`upstream/` 서브모듈(원작 socquique/TamaPoke를 고정 참조)이 아니라 별도
커뮤니티 포크인 [ShadowEnemyx/TamaPoke](https://github.com/ShadowEnemyx/TamaPoke)
("TamaPoke — Expanded")에서 포팅했습니다 — 원작에는 이 기능들이 없습니다.
정확히 어떤 파일을 가져왔는지는
[`upstream-expanded/README.md`](upstream-expanded/README.md) 참고. 박스
화면은 야생 배틀에서 이기고 잡은 개체만큼 채워집니다.

**도감이 151종이 아니라 386종(1~3세대)입니다.** 늘어난 건 목록뿐입니다:
1세대 항목은 기존과 바이트 단위로 동일하고, 희귀도·진화·스탯·배틀 규칙은
아무것도 바뀌지 않았습니다. 추가된 항목은 세 번째 포크인
[DylanPDao/TamaPoke](https://github.com/DylanPDao/TamaPoke)에서 옮겨온
것이며, 무엇을 가져오고 무엇을 가져오지 않았는지는
[`upstream-expanded/README.md`](upstream-expanded/README.md)에 정리해뒀습니다.
기존 세이브는 등록해둔 종을 그대로 유지합니다.

**사운드**는 녹음이 아니라 소프트웨어로 재합성한 것으로, 포크 자체의
audio.cpp가 만들어내는 것과 동일한 칩튠 파형(사각파/삼각파/소프트파/노이즈,
슬라이드와 효과별 볼륨 포함)입니다 — 자세한 방법은 [포팅 방식](#포팅-방식)
참고.

**한글이 7번째 언어로 추가됐습니다** — 기존 ES/EN/FR/DE/IT/PT에 이어서이며,
설정 화면에서 언어를 순환시키면 됩니다. 다른 여섯 언어와 달리 한글은
시스템 모노스페이스 폰트에 글리프가 없어 비율 폰트로 폴백되기 때문에,
UI의 모든 정렬/중앙정렬 계산을 `size*6`px 고정폭 가정 대신 실제 글자 폭
실측으로 바꿨습니다(이유는
[`kor_patch/FEASIBILITY.ko.md`](kor_patch/FEASIBILITY.ko.md), 전반적인
한글화 작업 기록은 [`kor_patch/`](kor_patch) 폴더 참고). 포켓몬 이름과
UI 문자열은 빌드에 포함돼 있고, 도감 설명문은 스프라이트와 동일한 방식으로
기존 개인별 다운로드 스크립트(`Scripts/fetch_dex_entries.sh --lang ko`)를
그대로 씁니다. **아직 실제 아이폰·워치에서 검증하지 않았습니다** — 문자열
테이블 크기, printf 포맷 지정자 순서/타입 일치, 컴파일러 문법 검사 같은
구조적 확인만 마친 상태이니, 누군가 실제로 한글로 플레이해보기 전까지는
막 붙인 상태로 봐주세요.

**의도적으로 다르게 만든 부분 하나:** 설정 화면에 수동 시계가 없습니다.
원본 하드웨어는 시간을 알 방법이 따로 없어서 손으로 시계를 맞추지만,
폰은 이미 시간을 알고 있어서 그냥 그걸 그대로 씁니다.

---

## 게임 방법 (실제 수치)

원작과 같은 엔진이라 수치도 동일합니다 — 이 표는
[원작 README](https://github.com/socquique/TamaPoke#game-manual-the-actual-numbers)의
표를 그대로 옮겨온 것입니다. 쉬운 설명 버전은 [게임 방법](docs/GAMEPLAY.ko.md)
참고.

**레벨업:** 실제 1분 = 게임 내 1분. 실제 **1시간마다 레벨 +1** — 잘 돌본다고
빨라지지 않고, 방치하면 진화가 *지연*됩니다. 앱을 닫아둬도 시간은 계속
흐르고(원본의 RTC처럼), 다시 열면 최대 **2주**까지 따라잡습니다.

**4개 스탯(0~100):** FOOD, JOY, ENE(기력), HYG(청결). 시작값 80/80/80/100,
깨어있는 동안 분당 FOOD −2, ENE −1(과체중이면 −1 추가), HYG −1(보이는
응가당 −4 추가), JOY −1(FOOD나 HYG가 30 미만이면 각각 −2 추가). 스탯이
10 이하가 되면 **관리 실패** — 진화가 1레벨 지연되고 유대감이 식습니다.

**행동:** 열매는 +25 FOOD(숨겨진 최애 맛이면 더 많이 + 유대감 상승); 사탕은
+10 FOOD/+12 JOY지만 체중 증가; 공 미니게임은 SPEED 훈련; 훈련 자루는
STRENGTH 훈련; 목욕은 응가 제거; 쓰다듬기는 JOY/유대감 소폭 상승; 수면은
모든 감소를 약 4배 늦추고 관리 실패·가출을 막아줌.

**알:** 첫 캐릭터는 1-3세대 스타터 9종 중에서 고르며, 한 페이지에 한
세대씩 넘겨서 선택합니다. 이후 알은 매번 희귀도를
굴리며(전설은 25종 이상 등록해야 등장 시작), 아직 완성 못한 진화 라인
쪽으로 편향되고, 스트릭·유대감이 좋으면 향상되며, 이로치 확률은 기본
**1/48**에서 **1/8**까지 올라갑니다.

**진화:** 레벨이 해당 종의 기준 이상이고 *동시에* 그 순간 모든 스탯이 40
이상이어야 합니다. 절대 자동으로 일어나지 않고, 항상 버튼을 눌러야 합니다.
1세대 6종(골뱃, 롱스톤, 럭키, 시드라, 스라크, 폴리곤)은 세대를 넘어
진화하며, 진화형이 4세대 이후에만 있는 종은 여기서 최종 진화형입니다.

**엔딩:** 작별(최종 진화형, 3일 경과, 직접 선택 — 다음 알에 축복), 가출(4개
스탯 전부 0으로 한 시간 — 다음 알에 저주), 놓아주기(아무 때나 길게 누르기).
셋 다 새 알로 이어집니다.

---

## 포팅 방식

펌웨어는 고정된 **466×466** 프레임버퍼에 그림을 그립니다. 이 좌표 공간을
그대로 유지한 채 아이폰/워치 화면 크기에 맞는 스케일 변환 하나만
적용했습니다 — 그래서 `drawScene`, `drawBars` 같은 그리기 함수들이 원본
C++에서 **한 줄 한 줄 그대로** 포팅됩니다.

게임 로직 자체는 **다시 작성하지 않았습니다.** 원본 `pet.cpp`와 `i18n.cpp`가
그대로 컴파일되며, Arduino/ESP32 전용 부분(타이머, 난수, 키/값 저장소)만
작은 셰임 헤더로 대체합니다. 얇은 브리지 레이어가 그 C++ 객체를 Swift UI에
노출할 뿐, 시뮬레이션·스탯 감소·진화 규칙·알 확률 중 어느 것도 재구현하지
않았습니다.

사운드도 마찬가지입니다: 펌웨어는 자체 오디오 칩에서 칩튠 파형(사각파,
삼각파, 더 부드러운 파형, 노이즈, 슬라이드와 효과별 볼륨까지 — Expanded
포크 자체의 audio.cpp)을 생성합니다. 여기엔 그런 칩이 없어서, 새로운 소리를
쓰는 대신 동일한 파형(같은 음표 테이블, 같은 샘플레이트, 같은 엔벨로프)을
소프트웨어로 합성합니다.

---

## 설치

코딩 배경지식이 전혀 없다고 가정한, 화면 단위로 자세한 전체 가이드:
**[docs/INSTALL.ko.md](docs/INSTALL.ko.md)**.

요약하면 — 쉬운 순서대로:

| | 필요한 것 | 지원 OS | 애플워치 | 비고 |
|---|---|---|---|---|
| **링크에서 웹앱 설치** (`browser_ver/`) | 폰 | 아이폰 / 아이패드 / 안드로이드 / 데스크톱 브라우저 | 없음 | 같은 게임, 같은 C++ 코어를 WebAssembly로 컴파일; Safari/Chrome에서 설치, 오프라인 동작, 만료 없음, 계정 불필요. 세이브와 스프라이트는 기기 안에 |
| **안드로이드 `.apk`** (같은 사이트) | 안드로이드 폰 | Android 8+ | 없음 | 웹 빌드를 진짜 앱(WebView 셸)으로 감싼 것, CI가 디버그 서명; 덮어 설치로 업데이트 |
| **서명 안 된 `.ipa`(워치 제외) + Sideloadly/AltStore** | 무료 Apple ID, Mac 불필요 | Mac 또는 **Windows** | 이 빌드엔 없음(아래 참고) | 네이티브 iOS 앱; 아이폰 전용; 7일마다 재서명 |
| **GitHub Actions로 서명된 `.ipa`** | 유료 Apple Developer 계정($99/년) | Mac 또는 Windows | 정상 설치 | 사이드로딩 도구 아예 불필요 |
| **Xcode로 직접 빌드** | Mac | Mac만 | 가장 안정적 | 이미 Xcode에 익숙한 분들에게 |

**무료 Apple ID로는 워치 앱을 사이드로드할 방법이 아예 없습니다** — 무료
프로비저닝 프로파일 하나로는 임베드된 두 번째 watchOS 앱까지 커버할 수
없기 때문입니다. 그래서 "Build (unsigned)" CI 작업은 IPA를 두 개
만듭니다: `TamaPoke-unsigned-nowatch.ipa`(아이폰 전용 — 이걸 받으세요)와
`TamaPoke-unsigned-withwatch.ipa`(참고용으로만 남겨둠 — AltStore는 이
파일 자체를 설치 거부하고, Sideloadly는 아이폰 앱만 설치하고 워치 부분은
빠뜨립니다). 유료 계정 없이 워치에도 넣고 싶다면 사이드로딩 대신 경로
C(Xcode로 워치와 직접 연결해 빌드)를 이용하세요.

어느 경로로 설치하든 **캐릭터 아트 없이** 설치됩니다 — [스프라이트](#스프라이트)
참고. 네이티브 iOS 앱은 파일 앱에서 읽고, 웹앱과 안드로이드 APK는 "Load
sprites…" 버튼으로 폰의 파일 선택 창을 열어 고른 뒤 앱 자체 저장소에
보관합니다.

---

## 스프라이트

앱은 포켓몬 아트를 내장한 채로 배포되지 않습니다 — 별도 라이선스가 있는
팬아트이기 때문입니다([라이선스](#라이선스) 참고). 그래서 어떤 빌드에도
포함되지 않고, 설치 후에 따로 추가합니다.

**간단한 방법 — 어떤 설치 방법으로 깔았든 동일하게 동작:** **파일** 앱 →
**내 iPhone → iTamaPoke** → `mons`라는 새 폴더 만들기 → `.bin` 스프라이트
파일 복사해 넣기([upstream 저장소](https://github.com/socquique/TamaPoke)의
`tools/sdcard/mons`에서 받으면 됨 — 정확한 클릭 위치는 [설치
가이드](docs/INSTALL.ko.md) 참고). 넣은 뒤 앱을 재시작하세요. `thumbs.bin`도
꼭 같이 넣어야 도감 화면이 텅 비지 않습니다. 애플워치도 페어링되어 있고
워치 앱도 설치했다면, 아이폰 앱을 잠깐 열어두면 자동으로 같은 파일을
워치로 보내려고 시도합니다 — 그 다음 워치 앱을 열면 바로 도착하는 경우가
많습니다.

**Xcode로 직접 빌드하는 경우:** `Scripts/fetch_sprites.sh`가 `upstream/`
서브모듈에서 스프라이트를 바로 빌드에 복사해 넣어서, 빌드하는 순간 앱
안에 내장됩니다. 정확한 명령어는 [docs/INSTALL.ko.md](docs/INSTALL.ko.md)의
"경로 C" 참고.

`upstream/`에는 1세대 151종만 들어 있습니다. 2-3세대 스프라이트와 모든
세대의 이로치는 PMD SpriteCollab에서 `Scripts/pack_shiny_sprites.py`(이로치)로
같은 폴더에 만들어 넣습니다. 이로치 파일이 없는 종은 평범한 색으로 부화하되
`*` 표시는 그대로 붙습니다.

파일이 없는 종은 화면이 깨지지 않고 그냥 플레이스홀더로 표시되므로,
일부만 넣어도 괜찮습니다.

---

## 도감 설명문

도감에서 포켓몬을 누르면 2페이지짜리 상세 화면이 열립니다 — 첫 페이지는
초상화와 타입 칩, 둘째 페이지는 도감 설명문입니다. 이 설명문은 Nintendo /
Game Freak의 저작물이라, 위의 스프라이트와 똑같이 **저장소에 포함하지
않습니다**. 화면은 `mons/dex_entries_<언어>.txt`를 읽습니다 — 현재 UI 언어에
맞는 파일 하나를 골라서 읽으며, 경로 규칙도 스프라이트와 같습니다:
`Documents/mons/`(파일 앱 → 내 iPhone 안에 → iTamaPoke)를 먼저 보고, 없으면
앱 번들을 봅니다.

`Scripts/fetch_dex_entries.sh`가 [PokéAPI](https://pokeapi.co)에서 언어별
파일을 하나씩 만들어줍니다 — 원작이 `dex.h`의 전투 스탯을 가져오는 것과 같은
출처입니다. 원하는 언어마다 한 번씩 실행하면 되고, 각 실행은 그 언어의
파일만 건드립니다:

```bash
Scripts/fetch_dex_entries.sh              # 전체 종, 영어
Scripts/fetch_dex_entries.sh --lang es    # 스페인어
Scripts/fetch_dex_entries.sh 1 4 7        # 이 셋만
```

형식은 한 줄에 하나씩 `<도감번호>|<설명문>`이고, 빈 줄과 `#`으로 시작하는
줄은 무시되므로 직접 작성해도 됩니다:

```
122|손으로 보이지 않는 벽을 만들어낸다.
```

현재 UI 언어에 맞는 파일이 없으면 해당 페이지에 `NO DEX ENTRY`라고만
표시되고, 나머지 화면은 그대로 동작합니다.

---

## 울음소리

같은 상세 화면 첫 페이지에서 짧은 울음소리도 들을 수 있습니다 — 초상화와
페이지 점(●○) 사이의 캡슐 버튼으로, 사운드 파일이 없는 종은 버튼 자체가
아예 안 보입니다. 스프라이트·도감 설명문과 마찬가지로 울음소리도 Nintendo /
Game Freak의 저작물이라 **저장소에 포함하지 않으며**, `mons/psnd<도감번호>.m4a`를
같은 방식으로 읽습니다(`Documents/mons/`를 먼저 보고, 없으면 앱 번들). 재생은
앱의 사운드 설정을 따르므로, 효과음을 꺼두면 울음소리도 같이 꺼집니다.

`Scripts/fetch_cries.sh`가 [PokéAPI의 cries 저장소](https://github.com/PokeAPI/cries)에서
파일을 만들어줍니다. iOS/watchOS의 AVFoundation이 Ogg Vorbis를 디코딩하지
못해서, 받아오는 김에 `ffmpeg`로 `.ogg`를 `.m4a`로 변환합니다. 먼저
`ffmpeg`를 설치하세요(`brew install ffmpeg`):

```bash
Scripts/fetch_cries.sh              # 전체 종
Scripts/fetch_cries.sh 1 4 7        # 이 셋만
```

`Scripts/fetch_assets.sh`는 스프라이트·이로치·울음소리를 한 번에 받아오는
래퍼입니다 — `Resources/mons/`에 들어갈 수 있는 걸 전부 한 번에:

```bash
Scripts/fetch_assets.sh                    # 전체 종, 모든 에셋
Scripts/fetch_assets.sh 1 4 7              # 이 셋만, 모든 에셋
Scripts/fetch_assets.sh --only sound       # 울음소리만
Scripts/fetch_assets.sh --only sprites,shiny 25 25   # 피카츄 스프라이트 둘만
```

---

## 저장과 파일 앱

아이폰 앱은 **파일 → 내 iPhone → iTamaPoke**에 `iTamaPoke-save.json`으로
저장 데이터를 두고, 저장할 때마다 자동으로 다시 씁니다. 백업하려면 이
파일을 복사해두거나 다른 기기로 옮기세요.

복원하려면 `iTamaPoke-import.json`이라는 이름으로 같은 위치에 넣고 앱을
여세요 — 한 번 로드된 뒤 이름이 바뀌어서 재시작해도 다시 불러와지지
않습니다. **이건 그 기기의 현재 캐릭터를 대체합니다.**

애플워치 앱은 완전히 별도의 저장 데이터를 가지며 파일 폴더가 없습니다.

---

## 앱 아이콘

기본적으로 작은 범용 마스코트 아이콘(포켓몬 아트 아님, 이 프로젝트를 위해
직접 그림)이 포함되어 배포됩니다. 본인 이미지를 쓰고 싶다면
`Scripts/fetch_app_icon.sh path/to/icon.png`로 교체하세요 — 자세한 내용은
[docs/INSTALL.ko.md](docs/INSTALL.ko.md)의 "앱 아이콘" 참고.

---

## 라이선스

- **이 포팅 자체의 코드**(Swift/watchOS 레이어, 브라우저 빌드, 스크립트,
  문서): **[LICENSE](LICENSE)** — 표준 오픈소스 라이선스가 아니라 짧은
  커스텀 라이선스입니다. 개인 이용과 수정은 자유롭고, 위 주소의 웹앱/APK를
  플레이하거나 본인 fork의 사본을 본인용으로 올리는 것도 됩니다. 어떤
  빌드든(리스킨 포함) 다시 배포하거나 남에게 넘기려면 먼저 저작권자의
  허락이 필요합니다. 길지 않으니 꼭 읽어보세요.
- **호스팅된 사이트가 실제로 내보내는 것**은 이 저장소에 있는 것과 같습니다:
  MIT 엔진 코드를 WebAssembly로 컴파일한 것, 이 포팅의 UI, 프로젝트용으로
  직접 그린 알 아이콘. 배포 워크플로는 폴더에 스프라이트·울음소리·도감
  설명문 파일이 하나라도 있으면 배포를 거부합니다.
- **원본 C++를 한 줄 한 줄 번역한 부분**(렌더러, 레이아웃 등): 여전히 ©
  Quique Tortosa, MIT — 이 저장소를 어떻게 받았든 함께 딸려오도록, MIT 원문
  자체를 [NOTICE](NOTICE)에 그대로 실어뒀습니다(MIT 조건 자체가 요구하는
  부분입니다).
- **`upstream/` 서브모듈 자체**: 저희가 재라이선스할 수 있는 대상이
  아닙니다 — [원본 저장소](https://github.com/socquique/TamaPoke)에서 직접
  받으면 그쪽 MIT 조건이 그대로 적용됩니다.
- **포켓몬 디자인, 그림, 도감 설명문, 울음소리**: © Nintendo / Game Freak /
  The Pokémon Company. **이 저장소, 그 빌드, 호스팅된 사이트 어디에도
  포함되어 있지 않습니다** — 설치 후 직접 받는 방법은
  [스프라이트](#스프라이트), [도감 설명문](#도감-설명문),
  [울음소리](#울음소리) 참고. 종 *표*(번호, 7개 언어 이름, 타입, PokéAPI
  기본 능력치, 진화 연결)는 MIT 원본에서 물려받은 데이터로, 코드가 담고
  있는 유일한 포켓몬 관련 내용입니다 — 이름은 사용자가 넣은 파일이 어느
  종의 것인지 표시하는 데 쓰입니다.
- **스프라이트 아트**: [PMD
  SpriteCollab](https://github.com/PMDCollab/SpriteCollab)(CC BY-NC
  4.0 — https://creativecommons.org/licenses/by-nc/4.0/), Nintendo와
  무관한 팬 제작 비영리 프로젝트입니다. 이것도 이 저장소에 포함돼 있지
  않습니다 — CC BY-NC 자체는 출처 표기만 하면 비영리 재배포를 허용하지만,
  위의 Nintendo 소유 자산(도감문, 울음소리)과 함께 받아오는 데다 그쪽은
  그런 허용이 없기 때문에, 이 프로젝트는 라이선스별로 따지지 않고 모든
  캐릭터 에셋에 "기기별로 직접 받고, 재배포하지 않는다"는 규칙을 똑같이
  적용합니다. 자세한 이유는 [NOTICE](NOTICE) 참고.

이 프로젝트는 비공식 팬 프로젝트이며 Nintendo와 무관하고 승인받지
않았습니다. 무엇이 포함되고 안 되는지 전체 정리는 [NOTICE](NOTICE) 참고.

---

## 크레딧

게임 디자인, 엔진, 원본 하드웨어: **Quique Tortosa**
([socquique/TamaPoke](https://github.com/socquique/TamaPoke)). 스프라이트:
[PMD SpriteCollab](https://github.com/PMDCollab/SpriteCollab). 배틀 스탯:
[PokéAPI](https://pokeapi.co). 포켓몬은 Nintendo / Game Freak / The Pokémon
Company의 상표입니다.

---

## Upstream

`37ba1c4`에 고정되어 있습니다. upstream 수정사항을 받아오려면:

```bash
git submodule update --remote upstream
```

그런 다음 다시 빌드하세요 — 셰임 레이어가 유일한 연결 지점이라 대부분의
upstream 변경은 이쪽에 비용이 들지 않습니다.
